colors = {
	# Norman Conquest situation map-mode side colors. map_ENG already exists
	# (named_colors/02_map.txt:15); these two follow its hsv360 form.
	# _1066 suffix: vanilla ships a map_NRM of its OWN (02_map.txt:217) and
	# CONSUMES it — NRM's country color (france.txt:169) and the norman
	# CULTURE's color (cultures/french.txt:492). An unsuffixed key here
	# silently repainted both (AUDIT-2026-07-31 D3). Tag-named keys must be
	# checked against vanilla KEY NAMES, not just rgb values.
	map_NRM_1066 = hsv360 { 45 85 90 }
	map_NOR = hsv360 { 215 70 55 }
	# Pereyaslavl, the first NEW_COUNTRIES tag.
	map_PYS = hsv360 { 190 55 65 }
	# The thirteen taifas. All rgb triples verified absent from vanilla
	# 02_map.txt (3744 map_* entries); chosen against their actual 1066
	# neighbors — CAS orange-gold, ARA dark red, POR blue, MOR tan,
	# map_GRA pale sage. map_SEV and map_TOL exist in vanilla as KEYS
	# (02_map.txt:88/:155 — zero consumers, but shadowing a vanilla key
	# is the D3 class) — renamed _1066 (AUDIT-2026-07-31 D3).
	map_SEV_1066 = rgb { 34 110 74 } # Seville      deep green
	map_CRD = rgb { 96 178 128 }    # Cordoba      light green (enclave in SEV)
	map_QRM = rgb { 158 200 150 }   # Carmona      pale green  (enclave in SEV)
	map_BDJ = rgb { 122 158 88 }    # Badajoz      olive
	map_TOL_1066 = rgb { 176 146 48 } # Toledo     ochre
	map_ALP = rgb { 120 200 200 }   # Alpuente     cyan        (enclave in TOL)
	map_ZGZ = rgb { 70 130 150 }    # Zaragoza     teal
	map_LRD = rgb { 200 210 90 }    # Lerida       chartreuse
	map_ABR = rgb { 168 60 150 }    # Albarracin   orchid
	map_GRZ = rgb { 140 46 104 }    # Granada      deep magenta
	map_ALM = rgb { 205 105 135 }   # Almeria      rose
	map_MRU = rgb { 90 70 150 }     # Murcia       indigo
	map_DYA = rgb { 46 52 104 }     # Denia        navy
	# The six Pyrenean counties, checked against map_CAT burnt orange,
	# map_ARA dark red, map_ANR olive, map_FRA blue.
	map_URG = rgb { 222 190 55 }    # Urgell       gold
	map_BSL = rgb { 118 82 158 }    # Besalu       violet
	map_CDY = rgb { 78 148 208 }    # Cerdanya     sky blue
	map_EPU = rgb { 58 148 128 }    # Empuries     teal-green
	map_RSL = rgb { 198 78 138 }    # Roussillon   pink
	map_PLJ = rgb { 112 88 58 }     # Pallars      brown
	# The Seljuk world. SEL itself wears vanilla's own map_seljukids
	# (30 160 203 — same VALUE as map_RUM, which is poetic: RUM is the
	# post-Manzikert successor). Neighbors: BYZ purple, GEO red,
	# MAM tan-ish, SRV existing.
	map_ABS = rgb { 45 45 45 }      # Abbasids     the black banner
	map_GHZ = rgb { 130 95 45 }     # Ghaznavids   bronze
	map_UQY = rgb { 185 150 80 }    # Mosul        sand
	map_MRD = rgb { 105 70 40 }     # Diyar Bakr   dark umber
	map_HLB = rgb { 200 175 90 }    # Aleppo       pale gold
	map_SIS = rgb { 155 120 90 }    # Sistan       dun
	map_KKY = rgb { 205 190 140 }   # Yazd         desert cream
	map_SHD = rgb { 95 135 95 }     # Arran        sage green
	# Fatimid Egypt. The dynastic banner was WHITE (unmappable); the
	# conventional Ismaili green, kept distinct from map_egypt
	# (67 176 140, reserved by the EGY formable) and from the Iberian
	# greens. Neighbors: BYZ purple, HLB pale gold, MEC/MDA vanilla.
	map_FAT = rgb { 24 134 90 }     # Fatimids     emerald
	# Vermandois — the checky or of its arms; neighbors FRA blue,
	# FLA and BLS vanilla, VLS/PIC-area greys.
	map_VMD = rgb { 186 148 58 }    # Vermandois   old gold
	# The British Isles slice. Neighbors checked: LEI dark green,
	# MTH cyan-ish, ULS orange (goes landless), AMA violet,
	# map_norse_gael teal (LOI's).
	map_DUB = rgb { 150 40 52 }     # Dublin       Norse crimson
	map_ULD = rgb { 205 170 100 }   # Ulaid        sand gold
	# Southern Italy. Neighbors: PAP vanilla, SIC dark olive
	# (rgb 100 100 0 — the Gallura-class eyeball if the emirate
	# greens read badly next to it), BYZ purple returns to Bari.
	map_APU = rgb { 170 34 40 }     # Apulia       Norman crimson
	map_CUP = rgb { 52 96 158 }     # Capua        steel blue
	map_SLR = rgb { 60 140 150 }    # Salerno      teal
	map_NEA = rgb { 235 190 80 }    # Naples       golden yellow
	map_GAE = rgb { 120 170 200 }   # Gaeta        pale blue
	map_PLM = rgb { 34 110 60 }     # Palermo      deep green
	map_AGR = rgb { 196 164 96 }    # Agrigento    sand
	# The HRE slice: Zähringen in the Breisgau (neighbors BDE/KYB
	# greys, ALS, the Swiss patchwork).
	map_ZAH = rgb { 178 60 60 }     # Zähringen    Breisgau red
	# Central Asia. QRK/QRA must read related-but-distinct (the
	# Cagliari/Gallura lesson); BLH bronze against map_golden_horde's
	# gold (hsv360 42 77 81) and SEL's map_seljukids cyan. All three
	# keys verified absent from vanilla named_colors BY KEY NAME
	# (zero substring hits — the D3 lesson).
	map_QRK = rgb { 40 90 160 }     # W. Karakhanids  steppe blue
	map_QRA = rgb { 96 150 205 }    # E. Karakhanids  pale blue (branch)
	map_BLH = rgb { 150 100 60 }    # Volga Bulgaria  Volga bronze
	# Arabia: the Qarmatian state of al-Hasa and the Ukhaydirid emirate
	# of al-Yamama. Checked against map_kaliji, map_omani, map_hijazi
	# and map_seljukids; both keys verified absent from vanilla BY KEY
	# NAME (zero substring hits — the D3 lesson).
	map_QMT = rgb { 168 62 78 }     # al-Hasa       Gulf carmine
	map_UKH = rgb { 122 104 176 }   # al-Yamama     Alid violet
	# The Cumans. NOT the package's ochre-gold suggestion: CUM borders
	# GLH's saturated amber (map_golden_horde, hsv360 42 77 81) along
	# its whole eastern edge — the Cagliari/Gallura lesson. A reddish
	# steppe dun, distinct from both GLH gold and BLH bronze; key
	# verified absent from vanilla BY NAME (the D3 lesson).
	map_CUM = rgb { 176 124 92 }    # Cumania       steppe dun
	# Northern Dynasties. LIA against CHI's crimson and KOR's blue
	# (map_KOR 26 53 177 — indigo shifted darker to stay distinct);
	# XIA takes vanilla's own map_tangut VALUE (02_map.txt:2434) so the
	# state reads as its culture. Keys verified absent from vanilla BY
	# NAME (zero substring hits — the D3 lesson).
	map_LIA = rgb { 40 60 110 }     # the Liao       Khitan indigo
	map_XIA = rgb { 170 95 126 }    # Western Xia    Tangut rose
	# India Tier 1. COZ vs map_pandya violet (TNK borders it); CLK vs
	# map_hoysala crimson and map_orissa orange; PAA vs map_GAU's
	# orange, which it replaces. Keys verified absent from vanilla BY
	# NAME (zero substring hits).
	map_COZ = rgb { 176 30 46 }     # Chola      tiger crimson
	map_CLK = rgb { 216 152 40 }    # Chalukya   Deccan ochre
	map_PAA = rgb { 190 108 30 }    # Pala       kasaya saffron-brown
	map_PMR = rgb { 92 132 60 }     # Paramara   Malwa green
	map_CHU = rgb { 60 128 176 }    # Solanki    Gujarat sea-blue
	# Tibet. DBU wears vanilla's own map_TIB (02_map.txt:2596, freed by
	# TIB's retirement â the PGN/map_burmese precedent). GTS against DBU's
	# pale sage, GUG's ochre and POO's violet; TKA against XIA's Tangut
	# rose, CHI's map_CSO crimson, AMD green and GOL sand. Both key names
	# verified absent from vanilla BY NAME (zero substring hits â D3).
	map_GTS = rgb { 62 118 130 }    # Tsang       plateau teal
	map_TKA = rgb { 196 126 44 }    # Tsongkha    Kokonor amber
}
