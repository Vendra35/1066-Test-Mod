# MEASUREMENT INSTRUMENT, not content - POP-PHASE-PACKAGE.md SA.6.
# One additive pop on SIC's mistretta (vanilla: exactly two sicilian/
# catholic pops, template catholic). Four outcomes, each deciding the
# pop-phase route; DELETE this file in the commit that records the
# result. Never launch it alongside another pop change.
locations = {
	mistretta = {
		define_pop = {	type = peasants	size = 60.000	culture = maltese	religion = sunni }
	}
}
